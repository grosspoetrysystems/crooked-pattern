> Skill: brownfield-compress | Source: .agents/skills/brownfield-compress/SKILL.md

Read and execute the skill definition at `.agents/skills/brownfield-compress/SKILL.md`.
If the skill has a `references/` directory, read relevant references as needed during execution.
If the skill has a `scripts/` directory, run scripts as instructed by the SKILL.md.
